#  Proof of Concept: Automated IT Grade Classification

## 1. Гипотеза (Hypothesis)

Уровень квалификации IT-специалиста (**Junior / Middle / Senior**) можно с высокой точностью определить на основе семантического анализа описания задач и стека технологий, **не опираясь на прямой стаж работы (в годах) и зарплатные ожидания**.

## 2. Методология эксперимента (Ablation Study)

Для подтверждения гипотезы был проведен "слепой" эксперимент. Из обучающих данных были принудительно удалены все признаки, которые могли бы дать модели прямую подсказку о стаже ("Target Leakage"):

1. **Удален стаж:** Колонка `total_experience_months` исключена из признаков ().
2. **Удалена зарплата:** Колонка `target_salary_rub` исключена, так как она имеет линейную корреляцию с грейдом.
3. **Очистка текста от цифр:** Из текстового описания опыта (перед подачей в BERT) удалены все цифры. Модель не видела фраз "5 лет опыта", "2015-2020", "команда из 10 человек".

**Используемые признаки:**

* **NLP Embeddings:** Векторные представления текста (опыт + навыки) через модель `rubert-tiny2` (размерность 256).
* **Hard Skills:** Бинарные признаки стека (Python, Java, Docker, K8s, SQL и др.), извлеченные через Regex.
* **Meta:** Город, тип занятости, образование.

## 3. Сравнение моделей

Были обучены три архитектуры. Метрика оптимизации — **Weighted F1-Score** (из-за дисбаланса классов: Senior > Middle > Junior).

| Модель | Accuracy | Weighted F1 | Junior Precision | Senior F1 |
| --- | --- | --- | --- | --- |
| **Logistic Regression** | 0.91 | 0.92 | 0.68 | 0.96 |
| **Random Forest** | 0.84 | 0.84 | 0.85 | 0.92 |
| **XGBoost (Winner)** 🏆 | **0.92** | **0.92** | **0.86** | **0.96** |

### Анализ:

* **Logistic Regression** показывает высокий средний балл, но плохо работает с классом **Junior** (Precision 0.68). Она часто путает сильных джунов с мидлами.
* **Random Forest** переобучился на доминирующем классе (Senior) и показал худший результат.
* **XGBoost** показал лучший баланс. Он удерживает **Precision 0.86** на джунах, что критически важно для бизнеса (фильтрация неквалифицированных кандидатов).

## 4. Детальный отчет (XGBoost Best Model)

Модель продемонстрировала устойчивость к отсутствию временных данных.

**Матрица метрик:**

```text
              precision    recall  f1-score   support

      Junior       0.86      0.67      0.75       648
      Middle       0.85      0.82      0.83      2056
      Senior       0.95      0.98      0.96      6578

    accuracy                           0.92      9282
   macro avg       0.88      0.82      0.85      9282
weighted avg       0.92      0.92      0.92      9282

```

### Интерпретация классов:

1. **Senior (F1: 0.96):** Модель определяет почти безошибочно. Сеньоры используют уникальный лексикон ("архитектура", "управление", "оптимизация", сложные инструменты), который модель легко считывает через BERT-эмбеддинги.
2. **Junior (Precision: 0.86):** Несмотря на то, что модель "пропускает" часть джунов (Recall 0.67), считая их мидлами (вероятно, из-за хорошего оформления резюме), **если модель ставит метку Junior — она права в 86% случаев**.
3. **Middle (F1: 0.83):** Стабильный результат. Класс хорошо отделяется от Senior, но имеет размытую границу с Junior при отсутствии данных о стаже.

## 5. Выводы

1. **Гипотеза подтверждена:** Мы доказали, что профессиональный уровень (Grade) закодирован в **описании задач и навыках**. У модели получилось восстановить грейд с точностью **92%** без знания стажа и зарплаты. Данное решение позволяет проводить автоматический скрининг резюме, исключая предвзятость, основанную на возрасте или формальном стаже работы. Связка `Regex для обработки скиллов` + `BERT Embeddings` + `XGBoost` является оптимальной для данной задачи.

---

### Как воспроизвести результат

1. **Подготовка данных:**
```bash
python app hh.csv
```


*Скрипт автоматически очистит текст, удалит цифры и сгенерирует эмбеддинги.*  
2. **Обучение:**
```bash
python train.py .
```


*Скрипт запустит GridSearch, выберет лучшие параметры и сохранит `best_model.pkl`.*  
3. **Инференс:**
```bash
python inference.py x_data.npy
```


*Вывод:* `['Senior', 'Middle', 'Junior', ...]`